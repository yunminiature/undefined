export const CANVAS_CONFIG = {
  PADDING: 8,
  TILE_BORDER_RADIUS: 12,
  MIN_FONT_SIZE: 16,
  MAX_FONT_SIZE: 32,
  FONT_SIZE_RATIO: 3,
} as const

export const SIZING = {
  MOBILE_MAX_SIZE: '24rem',
  DESKTOP_MAX_SIZE: '36rem',
} as const

export const FONT_CONFIG = {
  FAMILY: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif',
  WEIGHT: 'bold',
} as const
