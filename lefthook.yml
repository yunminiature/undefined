pre-commit:
  parallel: true
  commands:
    eslint:
      glob: '*.{ts,tsx,js,jsx}'
      run: npx eslint --ext ts --ext tsx --max-warnings=60 {staged_files}
    prettier:
      run: npx prettier --write {staged_files} && git add {staged_files}
